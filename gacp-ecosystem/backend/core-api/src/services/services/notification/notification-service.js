/**
 * Notification Service Stub
 * Placeholder for queue service dependency
 */

module.exports = {
    notifyNewApplication: async () => ({ success: true }),
    notifyInspectorAssignment: async () => ({ success: true }),
    notifyApproverInspectionComplete: async () => ({ success: true }),
    notifyFarmerStatusChange: async () => ({ success: true }),
    sendInspectionReminder: async () => ({ success: true }),
};
