# GACP Platform - Production Environment Configuration
# Rename this file to .env.production on your server

# 1. Server Configuration
NODE_ENV=production
PORT=3000
API_BASE_URL=https://api.gacp-platform.com # Replace with actual domain

# 2. Database (MongoDB Atlas)
# Ensure this user has read/write access to the production database
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/gacp-production?retryWrites=true&w=majority

# 3. Security (JWT)
# Generate weak keys for dev, but USE STRONG RANDOM STRINGS IN PRODUCTION
# Command to generate: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_TO_A_VERY_LONG_RANDOM_STRING_FOR_PUBLIC_USERS
FARMER_JWT_SECRET=CHANGE_ME_TO_A_VERY_LONG_RANDOM_STRING_FOR_PUBLIC_USERS
DTAM_JWT_SECRET=CHANGE_ME_TO_A_DIFFERENT_VERY_LONG_RANDOM_STRING_FOR_STAFF

# 4. Storage (Local or S3)
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./uploads

# 5. Payment Gateway (Ksher)
KSHER_MERCHANT_ID=<your_merchant_id>
KSHER_APP_ID=<your_app_id>
KSHER_PRIVATE_KEY_PATH=./keys/ksher_private.pem

# 6. Logging
LOG_LEVEL=info
