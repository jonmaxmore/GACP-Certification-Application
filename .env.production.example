# ============================================================
# GACP Platform - Production Environment Variables
# ============================================================
# Copy this file to .env.production and update values
# DO NOT commit .env.production to git!
# ============================================================

# ------------------------------------------------------------
# DATABASE (PostgreSQL)
# ------------------------------------------------------------
DB_USER=gacp
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
DB_NAME=gacp_db
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}?schema=public

# ------------------------------------------------------------
# CACHE (Redis)
# ------------------------------------------------------------
REDIS_URL=redis://redis:6379

# ------------------------------------------------------------
# SECURITY (JWT)
# ------------------------------------------------------------
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_WITH_OPENSSL_RAND_BASE64_64
DTAM_JWT_SECRET=CHANGE_THIS_WITH_DIFFERENT_SECRET

# ------------------------------------------------------------
# PUBLIC URLS (IP / HTTP Mode for Testing)
# ------------------------------------------------------------
PUBLIC_API_URL=http://47.129.167.71
PUBLIC_WEB_URL=http://47.129.167.71
PUBLIC_STAFF_URL=http://47.129.167.71/staff

# For production (replace with real domain)
# PUBLIC_API_URL=https://api.gacp-platform.com
# PUBLIC_WEB_URL=https://farmer.gacp-platform.com
# PUBLIC_STAFF_URL=https://staff.gacp-platform.com

# ------------------------------------------------------------
# OPTIONAL: Email/SMS Services
# ------------------------------------------------------------
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=no-reply@gacp-platform.com
# SMTP_PASSWORD=your-smtp-password
# SMS_API_KEY=your-sms-provider-api-key

# ------------------------------------------------------------
# OPTIONAL: File Storage (S3-compatible)
# ------------------------------------------------------------
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_BUCKET=gacp-documents
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_REGION=ap-southeast-1
